{
  "name": "Iron Condor",
  "description": "A neutral income strategy that sells an OTM call spread and an OTM put spread on the same expiry. Profits from time decay and low volatility when the underlying stays inside the profit zone. Max profit is the net credit; max loss is the spread width minus the credit.",
  "category": "options",
  "tags": ["options", "iron-condor", "neutral", "credit-spread", "income", "theta-decay"],
  "asset_types": ["options"],
  "visibility": "public",
  "version": 1,
  "visual_blocks": [
    {
      "id": "trigger-low-iv",
      "type": "trigger",
      "name": "Bollinger Squeeze",
      "description": "Low volatility environment — ideal for selling premium",
      "icon": "Minimize2",
      "inputs": [
        { "id": "period", "name": "BB Period", "type": "number", "required": true, "defaultValue": 20 },
        { "id": "std_dev", "name": "Std Dev", "type": "number", "required": true, "defaultValue": 2.0 }
      ],
      "outputs": [{ "id": "signal", "name": "Signal", "type": "signal" }],
      "config": {},
      "position": { "x": 100, "y": 180 }
    },
    {
      "id": "action-sell-call-spread",
      "type": "action",
      "name": "Sell",
      "description": "Sell OTM call spread (short +5% call, long +10% call)",
      "icon": "ArrowDownCircle",
      "inputs": [
        { "id": "trigger", "name": "Trigger", "type": "signal", "required": true },
        { "id": "quantity", "name": "Contracts", "type": "number", "required": true, "defaultValue": 1 }
      ],
      "outputs": [{ "id": "executed", "name": "Executed", "type": "signal" }],
      "config": { "spread_type": "bear_call", "short_offset_pct": 5, "long_offset_pct": 10, "expiration_days": 45 },
      "position": { "x": 400, "y": 100 }
    },
    {
      "id": "action-sell-put-spread",
      "type": "action",
      "name": "Sell",
      "description": "Sell OTM put spread (short -5% put, long -10% put)",
      "icon": "ArrowDownCircle",
      "inputs": [
        { "id": "trigger", "name": "Trigger", "type": "signal", "required": true },
        { "id": "quantity", "name": "Contracts", "type": "number", "required": true, "defaultValue": 1 }
      ],
      "outputs": [{ "id": "executed", "name": "Executed", "type": "signal" }],
      "config": { "spread_type": "bull_put", "short_offset_pct": -5, "long_offset_pct": -10, "expiration_days": 45 },
      "position": { "x": 400, "y": 280 }
    },
    {
      "id": "trigger-exit",
      "type": "trigger",
      "name": "Time-Based",
      "description": "Close all legs at 21 DTE",
      "icon": "Clock",
      "inputs": [
        { "id": "time", "name": "Time", "type": "string", "required": true, "defaultValue": "09:35" },
        { "id": "days", "name": "Days", "type": "string", "required": true, "defaultValue": "weekdays" }
      ],
      "outputs": [{ "id": "signal", "name": "Signal", "type": "signal" }],
      "config": { "days_to_expiration_exit": 21 },
      "position": { "x": 100, "y": 460 }
    }
  ],
  "connections": [
    { "id": "conn-1", "sourceBlockId": "trigger-low-iv", "sourceOutputId": "signal", "targetBlockId": "action-sell-call-spread", "targetInputId": "trigger" },
    { "id": "conn-2", "sourceBlockId": "trigger-low-iv", "sourceOutputId": "signal", "targetBlockId": "action-sell-put-spread", "targetInputId": "trigger" }
  ],
  "code": "",
  "performance_summary": {
    "sharpe_ratio": 1.45,
    "total_return": 38.2,
    "max_drawdown": 12.4
  },
  "author": "Agentic Trading"
}
